export type BridgeConstruction = {
  numbe: number;
  name: string;
  time_first_wiring: string;
  the_first_arch: string;
  the_second_wiring: string;
  the_second_arch: string;
  coordinates: [number, number];
};
